name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  checks:
    name: Checks
    runs-on: ubuntu-latest
    env:
      VORYN_TEST_RPC_URL: ${{ vars.VORYN_TEST_RPC_URL }}
      VORYN_TEST_CHAIN_ID: ${{ vars.VORYN_TEST_CHAIN_ID }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Prepare environment
        run: cp dev/.env.example dev/.env

      - name: Install dependencies
        run: make install

      - name: Start database
        run: make db-up

      - name: Build package
        run: make build

      - name: Build tests
        run: make build-test

      - name: Lint
        run: make lint

      - name: Unit tests
        run: make test

      - name: Coverage
        run: make test-coverage

      - name: Unit coverage
        run: make test-unit-coverage

      - name: Check live RPC configuration
        run: |
          if [ -z "$VORYN_TEST_RPC_URL" ]; then
            echo "VORYN_TEST_RPC_URL variable is required"
            exit 1
          fi
          if [ -z "$VORYN_TEST_CHAIN_ID" ]; then
            echo "VORYN_TEST_CHAIN_ID variable is required"
            exit 1
          fi

      - name: Live RPC tests
        run: make test-live-rpc

      - name: Clean up
        if: always()
        run: make down

  release:
    name: Release
    runs-on: ubuntu-latest
    needs: checks
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      issues: write
      pull-requests: write
      id-token: write
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      GIT_NAME: github-actions[bot]
      GIT_EMAIL: 41898282+github-actions[bot]@users.noreply.github.com

    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Release
        run: npm run release
